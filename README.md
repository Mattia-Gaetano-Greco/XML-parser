# XML-parser
XML parser for Java (with examples)

Simple parser for Java for a school project.

This project models the reality of a laboratory with multiple PCs, and each PC has some characteristics.

It tests the XML against the pre-defined XSD (returning an error if it results in false).
It then prints the characteristics of each PC and prints which one has the best characteristics